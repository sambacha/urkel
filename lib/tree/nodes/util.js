"use strict";

/*
 * Constants
 */

const NULL = 0;
const INTERNAL = 1;
const LEAF = 2;
const HASH = 3;

const types = {
  NULL,
  INTERNAL,
  LEAF,
  HASH,
};

const typesByVal = ["NULL", "INTERNAL", "LEAF", "HASH"];
